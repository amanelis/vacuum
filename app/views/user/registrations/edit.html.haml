.row
  .large-12.columns.large-centered
    .panel
      %h2
        Edit #{resource_name.to_s.humanize}
      = form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f|
        = devise_error_messages!
        .row
          .large-6.columns
            = f.label :email
            = f.email_field :email
            = f.label :username
            = f.text_field :username

            = f.label :email_subscribed
            = f.select(:subscribed, options_for_select([true, false], resource.subscribed))
            
          .large-6.columns
            = f.label :password do
              Password
              %span.sub-label (leave blank if you don't want to change it)
            = f.password_field :password, :autocomplete => "off"
            = f.label :password_confirmation
            = f.password_field :password_confirmation
            = f.label :current_password do
              Current Password
              %span.sub-label (we need your current password to confirm your changes)
            = f.password_field :current_password
            = f.submit 'Update', :class => 'button round success'
            = link_to "Cancel", :back

        %p
          Unhappy? #{link_to "Cancel my account", registration_path(resource_name), :data => { :confirm => "Are you sure?" }, :method => :delete}.
